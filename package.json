{
  "devDependencies": {
    "jest": "^30.2.0",
    "esbuild": "^0.19.0"
  },
  "dependencies": {
    "email-addresses": "^3.1.0"
  },
  "scripts": {
  "test": "jest",
  "build": "esbuild src/services/authService.js --bundle --outfile=dist/service-worker.js --platform=browser --format=esm",
  "build:watch": "esbuild src/services/authService.js --bundle --outfile=dist/service-worker.js --platform=browser --format=esm --watch"
  }
}
