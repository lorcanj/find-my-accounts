document.addEventListener('DOMContentLoaded', () => {
  const scanButton = document.getElementById('scanGmail');
  scanButton.addEventListener('click', () => {
    // Trigger Gmail scan here
  });
});